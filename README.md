\# TP DevOps — Infrastructure \& Deployment



> DevOps TP — UCA ISIMA — Imrane Moumad



This repository automates the infrastructure provisioning and application deployment for the \*\*ACNHCollector\*\* project (a Vue.js frontend + Spring Boot backend).



\## 🏗️ Architecture



```mermaid

flowchart TB

&#x20;   subgraph DEV \[Developer's machine]

&#x20;       TOFU\[OpenTofu<br/>Creates VM, DB, DNS<br/>Writes DB creds to OpenBAO]

&#x20;       ANS\[Ansible<br/>local or CI<br/>Reads secrets from OpenBAO<br/>Deploys Traefik and apps]

&#x20;   end



&#x20;   subgraph BAO \[OpenBAO]

&#x20;       SECRETS\[home/imrane.moumad<br/>+ home/imrane.moumad/mysql]

&#x20;   end



&#x20;   subgraph VM \[EduInfra VM]

&#x20;       TRAEFIK\[Traefik :80]

&#x20;       BACK\[Backend<br/>Spring Boot]

&#x20;       FRONT\[Frontend<br/>Vue / nginx]

&#x20;       TRAEFIK --> BACK

&#x20;       TRAEFIK --> FRONT

&#x20;   end



&#x20;   subgraph CC \[Clever Cloud]

&#x20;       DB\[(MySQL free tier)]

&#x20;   end



&#x20;   subgraph OVH \[OVH DNS]

&#x20;       DNS1\[imrane.moumad.uca-devops.ovh]

&#x20;       DNS2\[api.imrane.moumad.uca-devops.ovh]

&#x20;   end



&#x20;   TOFU -.provisions.-> VM

&#x20;   TOFU -.provisions.-> CC

&#x20;   TOFU -.provisions.-> OVH

&#x20;   TOFU -.writes.-> BAO

&#x20;   ANS -.reads.-> BAO

&#x20;   ANS -.configures.-> VM

&#x20;   BACK -->|JDBC| DB

&#x20;   DNS1 --> TRAEFIK

&#x20;   DNS2 --> TRAEFIK

```



\## 📂 Repository layout



```

infra/

├── tofu/                      # OpenTofu (Infrastructure as Code)

│   ├── providers.tf           # clevercloud, uca, ovh, local, vault

│   ├── variables.tf           # Input variables (from env vars)

│   ├── database.tf            # Clever Cloud MySQL add-on

│   ├── vm.tf                  # VM + security group + firewall rules

│   ├── dns.tf                 # OVH DNS records (frontend + api)

│   ├── ansible\_inventory.tf   # Auto-generates Ansible inventory

│   ├── ansible\_vars.tf        # Writes DB creds to OpenBAO

│   └── outputs.tf             # Prints VM IP, DNS, SSH command

│

├── ansible/                   # Ansible (Configuration Management)

│   ├── ansible.cfg

│   ├── playbook.yml

│   ├── inventories/prod/

│   │   └── inventory          # Auto-generated by OpenTofu (DNS-based)

│   └── tasks/

│       ├── load\_secrets.yml   # Pulls GitLab token + DB creds from OpenBAO

│       ├── install\_docker.yml

│       ├── registry\_login.yml

│       ├── network.yml

│       ├── deploy\_traefik.yml

│       ├── deploy\_backend.yml

│       └── deploy\_frontend.yml

│

├── .gitlab-ci.yml             # GitLab CI: runs Ansible on push to main

├── load-secrets.ps1           # Windows helper (PowerShell, gitignored)

├── load-secrets.sh            # Linux/WSL helper (bash, gitignored)

├── .env                       # OPENBAO\_TOKEN for WSL shell (gitignored)

└── .gitignore

```



\## 🔐 Secret management



All secrets live in \*\*OpenBAO\*\* — nothing in Git, nothing on disk long-term.



| Secret                   | Consumer  | Loaded by                               |

|--------------------------|-----------|-----------------------------------------|

| `CC\_ORGANISATION/\*`      | OpenTofu  | `load-secrets.\*` → `TF\_VAR\_\*` env       |

| `ovh\_\*` (4 keys)         | OpenTofu  | `load-secrets.\*` → `TF\_VAR\_\*` env       |

| `UCA\_USER\_TOKEN`         | OpenTofu  | `load-secrets.\*` → `TF\_VAR\_\*` env       |

| `ssh\_public\_key`         | OpenTofu  | `load-secrets.\*` → `TF\_VAR\_\*` env       |

| `GITLAB\_REGISTRY\_TOKEN`  | Ansible   | `tasks/load\_secrets.yml` (REST call)    |

| `MYSQL\_\*` (5 keys)       | Ansible   | `tasks/load\_secrets.yml` (REST call)    |



MySQL credentials are \*\*written to OpenBAO by OpenTofu\*\* after each `tofu apply` — Ansible always reads the current values, no manual sync.



Two equivalent secret loaders are provided (both gitignored):



\- `load-secrets.ps1` — PowerShell, for Windows where OpenTofu runs

\- `load-secrets.sh` — bash, for WSL/Linux where Ansible runs



\## 🚀 Usage



\### First-time setup



1\. Populate OpenBAO at `home/imrane.moumad` with these keys:

&#x20;  - `CC\_ORGANISATION`, `CC\_OAUTH\_TOKEN`, `CC\_OAUTH\_SECRET`

&#x20;  - `ovh\_application\_key`, `ovh\_application\_secret`, `ovh\_consumer\_key`, `ovh\_endpoint`

&#x20;  - `UCA\_USER\_TOKEN`

&#x20;  - `ssh\_public\_key`, `ssh\_private\_key`

&#x20;  - `GITLAB\_REGISTRY\_TOKEN`



2\. Generate an SSH keypair and upload the public key to OpenBAO as `ssh\_public\_key`:

```bash

&#x20;  ssh-keygen -t ed25519 -f \~/.ssh/tp\_vm\_key

```



\### Loading secrets — choose the script that matches your OS



Two equivalent helper scripts are provided (both gitignored, for local use only):



| OS              | Script              | How to run                                 |

|-----------------|---------------------|--------------------------------------------|

| \*\*Windows\*\*     | `load-secrets.ps1`  | `. .\\load-secrets.ps1` (PowerShell)        |

| \*\*Linux / WSL\*\* | `load-secrets.sh`   | `source ./load-secrets.sh` (bash)          |



Both scripts do the same thing: fetch all secrets from OpenBAO and export them as `TF\_VAR\_\*` / `UCA\_USER\_TOKEN` / `VAULT\_TOKEN` environment variables so OpenTofu and Ansible can read them.



If your `OPENBAO\_TOKEN` isn't already set in the shell, the script prompts for it interactively. You can also \*\*pass the token as an argument\*\* to skip the prompt:



```powershell

\# Windows

. .\\load-secrets.ps1 "s.abcdef123456"

```



```bash

\# Linux / WSL

source ./load-secrets.sh "s.abcdef123456"

```



> ⚠️ Passing the token as an argument stores it in your shell history. Use only in scripts or disposable sessions.



\### Deploy the infrastructure



> \*\*Note:\*\* Per the brief, \*\*OpenTofu is NOT run in CI\*\* — always run it locally.



\*\*Windows (PowerShell):\*\*

```powershell

. .\\load-secrets.ps1



cd tofu

tofu init     # first time only

tofu apply

```



\*\*Linux / WSL (bash):\*\*

```bash

source ./load-secrets.sh



cd tofu

tofu init     # first time only

tofu apply

```



OpenTofu creates the VM, DB, DNS records, writes MySQL credentials to OpenBAO, and regenerates the Ansible inventory automatically.



\### Deploy the application



\*\*Windows (PowerShell):\*\*

```powershell

. .\\load-secrets.ps1

cd ansible

ansible-playbook playbook.yml

```



\*\*Linux / WSL (bash):\*\*

```bash

source ./load-secrets.sh

cd ansible

ansible-playbook playbook.yml

```



Or simply \*\*push to `main`\*\* on the `infra` repository — GitLab CI runs the same Ansible playbook against the VM automatically.



\### Destroy (for submission / cleanup)



```bash

cd tofu

tofu destroy

```





\## 🧩 Tech stack



| Layer                | Tool                                       |

|----------------------|--------------------------------------------|

| IaC                  | OpenTofu 1.8+                              |

| Configuration Mgmt   | Ansible 2.16+                              |

| Secret Management    | OpenBAO (Vault-compatible)                 |

| Container Runtime    | Docker                                     |

| Reverse Proxy        | Traefik v3.6                               |

| CI/CD                | GitLab CI (Docker-in-Docker)               |

| VM Provider          | EduInfra (UCA)                             |

| Database Provider    | Clever Cloud (MySQL, free DEV tier)        |

| DNS Provider         | OVH (`uca-devops.ovh` zone)                |



\## 🌐 Live URLs



\- Frontend: http://imrane.moumad.uca-devops.ovh

\- API: http://api.imrane.moumad.uca-devops.ovh



\## 📌 Design notes



\- \*\*EduInfra VMs have a 4-hour lifetime\*\* (extendable). After recreation, `tofu apply` refreshes the inventory and OpenBAO secrets; Ansible then redeploys.

\- \*\*OpenTofu never runs in CI\*\* (teacher rule). Only the `infra` repo's CI runs Ansible against an existing VM.

\- `.terraform.lock.hcl` is committed so CI always uses identical provider versions.

\- Each repository (`backend`, `frontend`, `infra`) has its own GitLab CI pipeline.

